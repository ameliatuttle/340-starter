<%# The Content Heading %>
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } 
%>

<%# A Message Block %>
<%# messages() %>

<%# Flash Message Block %>
<% if (flashMessage) { %>
    <div class="alert alert-success">
        <%= flashMessage %>
    </div>
<% } %>

<h2>You're logged in!</h2>